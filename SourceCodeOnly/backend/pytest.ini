[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    unit: Fast-running unit tests
    integration: Integration tests with third party services/databases
    slow: Tests that take a long time to run (e.g. document processing pipeline runs)
